{
  "name": "spice-route-ops",
  "version": "1.0.0",
  "private": true,
  "description": "Spice Route Kitchens · Operations Pulse — Area Manager dashboard and read-only API over the Linemate extract",
  "main": "server.js",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "etl": "cd etl && python3 build.py",
    "report": "cd etl && python3 charts.py && python3 report.py && node make_docx.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  }
}
